# Logs
logs
*.log
npm-debug.log*

# Runtime data
pids
*.pid
*.seed

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules
jspm_packages

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history
.DS_Store

#Excluding build directory
 public/

# If the parent .gitignore ignore (PUBLIC) then Git never even looks inside public/ if the parent already told it to ignore the whole folder.

# Adding client prefix(as it points to client folder) before the public folders worked, as it points to directories and files correctly 🔥
# Ignore everything in public folder
client/public/*

# But don't ignore the folder itself
!client/public/

# Ignore not whole public files But NOT these files/folders
!client/public/images/
!client/public/favicon.ico
!client/public/index.html
!client/public/manifest.json 
!client/public/robots.txt
!client/public/.gitkeep 
##########################################################